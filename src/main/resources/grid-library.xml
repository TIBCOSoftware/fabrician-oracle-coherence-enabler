<?xml version="1.0" encoding="UTF-8"?>
<grid-library>
    <grid-library-name>oracle-coherence-enabler</grid-library-name>
    <grid-library-version>${project.version}</grid-library-version>

    <!-- No JRE arguments -->

    <dependency>
        <grid-library-name>oracle-coherence-distribution</grid-library-name>
        <grid-library-version>${distro-version}</grid-library-version>
    </dependency>

    <!-- No conflicts -->

    <!-- No jar paths -->

    <!-- No library paths -->

    <!-- No assembly paths -->

    <!-- No command paths -->

    <!-- No hooks paths -->

    <!-- No environment variables -->

    <!-- No system properties -->

</grid-library>
