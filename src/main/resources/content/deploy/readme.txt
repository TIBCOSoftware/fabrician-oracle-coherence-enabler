In order to allow dynamic deployment of code from a domain in the simplest
possible way, the enabler will extract any archives in a domain to this 
folder, which is on the classpath used by Coherence on startup. 